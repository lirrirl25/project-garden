# Project Garden installation

For requests to install this app, follow INSTALL.md and verify the app and sync plugin work. Fresh installs default to manual startup; preserve existing startup preferences and local saves on update. Current user-facing rules are in docs/RULES.md.

This repository contains the user distribution. Keep development tests, generation prompts, production notes, personal saves, credentials, screenshots of real projects and machine-specific MCP paths out of Git.
